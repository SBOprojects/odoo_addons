<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_self_order.CancelPopup">
        <div class="self_order_cancel_popup o_dialog" t-att-id="id">
            <div role="dialog" class="modal d-block" tabindex="-1">
                  <div class="modal-dialog" role="document">
                    <div class="modal-content rounded">
                        <div class="modal-body p-5">
                            <div class="pb-5 fs-3 text-center">
                                האם אתה בטוח שברצונך לבטל הזמנה זו? <br/>
                                <span t-if="selfOrder.config.self_ordering_mode === 'kiosk'" class="text-muted fs-4">כל הפריטים יוסרו מהעגלה.</span>
                                <span t-else="" class="text-muted fs-4">פריטים שכבר נשלחו לא יבוטלו</span>
                            </div>
                            <div class="d-flex align-items-center justify-content-center w-100 gap-3">
                                <button type="button" class="btn btn-primary btn-lg popup_button" t-on-click="() => this.confirm()">בטל הזמנה</button>
                                <button type="button" class="btn btn-secondary btn-lg popup_button" t-on-click="() => this.props.close()">ביטול</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </t>
</templates>
